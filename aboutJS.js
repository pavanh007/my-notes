/*
// * About JS
// * 1. lightweight, 2. interpretened, 3. I/O operations, 4. JIT compiler, 5.single threaded
// * JIT(just-in-compiler), is an inventory management method in which goods are received from suppliers only as they are needed.
// * It's used in both frontend framework like react, Vue, etc.. as we'll as backend using NodeJS.
*/

console.log("hello world");
const count = 5;
console.log("count: %d", count);
console.table(console);